# วิธีเอาโค้ดขึ้น GitHub (สำหรับมือใหม่)

ทำตามทีละขั้นตอนนะครับ ไม่ยาก!

## 1. สร้างพื้นที่เก็บโค้ด (Repository) บนเว็บ GitHub
1.  ไปที่เว็บ [github.com](https://github.com) (ล็อกอินให้เรียบร้อย)
2.  มุมขวาบน กดเครื่องหมาย **+** เลือก **"New repository"**
3.  ช่อง **Repository name**: ตั้งชื่อเป็นภาษาอังกฤษ (เช่น `my-game-community`)
4.  ส่วนอื่นไม่ต้องติ๊กอะไรเพิ่ม
5.  กดปุ่มสีเขียว **"Create repository"**

---

## 2. เอาโค้ดขึ้น (ผ่านโปรแกรม VS Code)
เราจะใช้หน้าต่าง Terminal ใน VS Code ของคุณนี่แหละครับ ง่ายดี

1.  ใน VS Code กดปุ่ม `Ctrl + J` เพื่อเปิดหน้าต่าง Terminal (ถ้ายังไม่เปิด)
2.  **ก๊อปปี้คำสั่งด้านล่างนี้** ไปวางทีละบรรทัด แล้วกด Enter:

```bash
git init
```
*(ถ้ามันบอกว่า Reinitialized existing Git repository แปลว่าเคยทำแล้ว ข้ามไปได้)*

```bash
git add .
```

```bash
git commit -m "First commit"
```

```bash
git branch -M main
```

**สำคัญมาก! บรรทัดนี้ต้องแก้:**
กลับไปที่หน้าเว็บ GitHub ที่เปิดค้างไว้ ก็อปปี้บรรทัดที่เขียนว่า `git remote add origin ...` มาวางครับ
*ตัวอย่าง (อย่าก๊อปอันนี้นะ ให้เอาจากเว็บของคุณ):*
`git remote add origin https://github.com/ชื่อคุณ/my-game-community.git`

```bash
git push -u origin main
```

---

## 3. กลับไปที่ Vercel
พอขึ้น GitHub เสร็จแล้ว
1.  กลับไปหน้า **Vercel**
2.  ลองกดค้นหาชื่อ Repository อีกรอบ (หรือกด Import จาก GitHub)
3.  คราวนี้จะเจอแล้วครับ! กด **Import** -> **Deploy** ได้เลย 🚀
