# วิธีเอาเว็บขึ้น Vercel (ขั้นตอนสุดท้าย)

1.  **กลับไปที่เว็บ Vercel Dashboard** ([vercel.com/dashboard](https://vercel.com/dashboard))
2.  กดปุ่ม **"Add New..."** (มุมขวาบน) -> เลือก **"Project"**
3.  ในหน้ารายชื่อ GitHub Repository:
    *   คุณจะเห็นชื่อ `game-community` ที่เพิ่งสร้างเมื่อกี้
    *   กดปุ่ม **"Import"** ข้างหลังชื่อนั้น
4.  **หน้า Configure Project**:
    *   **Framework Preset**: น่าจะขึ้น Next.js อัตโนมัติ (ถูกต้องแล้ว)
    *   **Root Directory**: ปล่อยว่างไว้
    *   **Environment Variables**:
        *   กดลูกศรลงมาเพื่อเปิด
        *   ใส่ค่าตามไฟล์ `.env.local` ของคุณ (เปิดไฟล์ใน VS Code ดูได้เลย)
        *   **สำคัญ!** ต้องใส่ `NEXT_PUBLIC_SUPABASE_URL` และ `NEXT_PUBLIC_SUPABASE_ANON_KEY` ไม่งั้นเว็บจะดึงข้อมูลเกมไม่ได้
5.  กดปุ่ม **"Deploy"**
6.  รอสักพัก... (ประมาณ 1-2 นาที) หน้าจอจะจุดพลุ 🎉

**ยินดีด้วย! เว็บคุณออนไลน์แล้ว ส่งลิงก์ให้เพื่อนดูได้เลย!**
